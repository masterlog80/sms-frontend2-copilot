# =============================================================================
# sms-frontend2-copilot — k3s / Kubernetes deployment
# =============================================================================
#
# RBAC note
# ---------
# The app queries the Kubernetes API at startup to auto-detect its image name
# and version.  The Role below grants the minimum required permission (get pods)
# in the DEFAULT namespace, which is where the pod actually runs when deployed
# via Portainer or a plain kubectl apply without a custom namespace.
#
# If you deploy into a different namespace, change every occurrence of
# "namespace: default" below to match.
#
# Quick-apply RBAC only (without redeploying):
#   kubectl apply -f rbac-default.yaml
#
# Full deploy:
#   kubectl apply -f k3s-deploy.yaml
# =============================================================================

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sms-dashboard-sa
  namespace: default

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: sms-dashboard-pod-reader
  namespace: default
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: sms-dashboard-pod-reader
  namespace: default
subjects:
  - kind: ServiceAccount
    name: sms-dashboard-sa
    namespace: default
roleRef:
  kind: Role
  apiGroup: rbac.authorization.k8s.io
  name: sms-dashboard-pod-reader

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sms-dashboard-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: modem-dashboard
  namespace: default
  labels:
    app: modem-dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: modem-dashboard
  template:
    metadata:
      labels:
        app: modem-dashboard
    spec:
      serviceAccountName: sms-dashboard-sa
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: sms-dashboard-data
      containers:
        - name: sms-dashboard
          image: modem-dashboard:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
              name: http
          env:
            - name: MODEM_DEVICE
              value: "/dev/ttyUSB2"
            - name: POLL_INTERVAL
              value: "5"
            - name: FORWARD_DELAY
              value: "30"
            - name: DATA_DIR
              value: "/data"
            - name: LOG_LEVEL
              value: "INFO"
            # IMAGE_NAME / IMAGE_VERSION auto-detected from k8s API via RBAC above
          volumeMounts:
            - name: data
              mountPath: /data
          securityContext:
            privileged: true       # required for USB modem device access
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            httpGet: { path: /api/status, port: 5000 }
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet: { path: /api/status, port: 5000 }
            initialDelaySeconds: 5
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: modem-dashboard
  namespace: default
spec:
  selector:
    app: modem-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: ClusterIP
